@model web.Models.ApplicationStatusIndicatorViewModel 

@if(Model.status)
{
    <span class="label label-success">@Model.name is Up</span>
}
else
{
    <span class="label label-important">@Model.name is Down</span>
}